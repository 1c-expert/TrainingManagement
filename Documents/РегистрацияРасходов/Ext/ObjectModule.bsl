
Процедура ОбработкаПроведения(Отказ, Режим)
	
	// регистр Расходы 
	Движения.Расходы.Записывать = Истина;
	Для Каждого ТекСтрокаРасходы Из Расходы Цикл
		Движение = Движения.Расходы.Добавить();
		Движение.Период = Дата;
		Движение.Спортсмен = Спортсмен;
		Движение.ВидСпорта = ТекСтрокаРасходы.ВидСпорта;
		Движение.СтатьяЗатрат = ТекСтрокаРасходы.СтатьяЗатрат;
		Движение.Затрата = ТекСтрокаРасходы.Затрата;
		Движение.Сумма = ТекСтрокаРасходы.Сумма;
	КонецЦикла;
	
КонецПроцедуры
