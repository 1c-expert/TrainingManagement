
Процедура ОбработкаПроведения(Отказ, Режим)
	// регистр РезультатыВзвешивания
	Движения.РезультатыИзмерений.Записывать = Истина;
	Для Каждого ТекСтрокаРезультаты Из Результаты Цикл
		Движение = Движения.РезультатыИзмерений.Добавить();
		Движение.Период = Дата;  
		Движение.Спортсмен = Спортсмен;
		Движение.Параметр = ТекСтрокаРезультаты.Параметр;
		Движение.ЗначениеПараметра = ТекСтрокаРезультаты.ЗначениеПараметра;
	КонецЦикла;

КонецПроцедуры
